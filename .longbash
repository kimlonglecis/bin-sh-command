export PATH=$PATH:$HOME/bin
export PATH="$HOME/.composer/vendor/bin:$PATH"
export DOTOKEN=3be472704a63f1beb649f64c10c9ebeba4bb781fd9ac69c5a0a2a86fb363c68a

# MIJ VARIABLES
export MIJ_DOCKER_PATH=/www/docker-composer-lamp5.6-jenkins-magento-modinjapan
export MIJ_BACKUP_PATH=/www/modinjapan-backup

# DOCKER MACHINE VARIABLE
export MIJ_MACHINE=modinjapan-magento-jenkins-docker-machine

# DATE TIME VARIABLES
export DATE=$(date +\%Y-\%m-\%d)
export TIME=$(date +\%H-%M-%S)
export DATETIME=$(date +\%Y-\%m-\%d--\%H-%M-%S)

# S3 BUCKET VARIABLES
DO_SPACES=brandcreative-docker-spaces
S3_BUCKET=s3://${DO_SPACES}/docker-machine
S3_BUCKET_MIJ_BACKUP=s3://${DO_SPACES}/docker-machine/${MIJ_MACHINE}/backup

# ALIAS
alias www="cd ~/Desktop/www"
alias wwww="cd /www"
alias bin="cd ~/bin"
